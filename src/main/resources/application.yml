spring:
  datasource:
    url: "jdbc:mariadb://localhost:3306/product_manager"
    username: "product_manager"
    password: 1234

  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 9090

logging:
  level:
    mosk.product: debug